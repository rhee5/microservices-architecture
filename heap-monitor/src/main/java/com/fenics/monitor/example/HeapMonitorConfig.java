package com.fenics.monitor.example;

/*
import com.fenics.monitor.model.AlertRunnable;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Primary;

@Configuration
public class HeapMonitorConfig {
    private static Logger logger = LogManager.getLogger(HeapMonitorConfig.class);

    @Bean
    @Primary
    public AlertRunnable alertRunnable() {
        return new AlertRunnable() {
            @Override
            public void run() {
                logger.warn("Replace this line with what needs to be executed when memory usage reaches the threshold");
            }
        };
    }
}
*/